{
  "name": "inteluitst",
  "version": "0.0.1",
  "description": "Testing Tony's intelui UI toolkit",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && npx tsc",
    "start": "npm run build && node dist/server/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean" : "npx rimraf static/dist"
  },
  "author": "R.G. Esteves",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "systemjs": "^0.21.5",
    "typescript": "^3.1.3"
  },
  "devDependencies": {
    "@types/express": "^4.16.0",
    "@types/node": "^10.12.0",
    "del": "^3.0.0"
  }
}

{
  "name": "inteluitst",
  "version": "0.0.1",
  "description": "Testing Tony's intelui UI toolkit",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && npx tsc -p src/client && npx tsc -p src/server",
    "start": "npm run build && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "npx rimraf static/js dist"
  },
  "author": "R.G. Esteves",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "systemjs": "^0.21.6",
    "typescript": "^3.5.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^10.14.15",
    "del": "^3.0.0"
  }
}
